<h3>Listado de Usuarios</h3>
<ol class="breadcrumb">
  <li>
    <%= link_to 'Inicio', root_path %>
  </li>
  <li>
    Usuarios
  </li>
  <li>
    <strong><%= link_to 'Añadir Usuario', new_user_path %></strong>
  </li>
</ol>

<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>Correo</th>

      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
        <td><%= user.email %></td>

        <td><%= link_to 'Ver', user %></td>
        <td><%= link_to 'Editar', edit_user_path(user) %></td>
        <td><%= link_to 'Eliminar', user, method: :delete, data: { confirm: 'Esta usted seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= form_for(@ip_status) do |f| %>
  <% if @ip_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ip_status.errors.count, "error") %> prohibited this ip_status from being saved:</h2>

      <ul>
      <% @ip_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status %><br>
    <%= f.text_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
